# gj-qa-release — Resources (curated foundation)

Curated, verified starter set for Unity QA, mobile release ops, and store policy. Expand to the full annotated top-100 in a live pass. All entries real/canonical.

## Unity testing
- Unity — Test Framework / UTF (docs.unity3d.com → Testing) — edit-mode + play-mode.
- Unity — Performance Testing Extension; Profiler + Memory Profiler (docs.unity3d.com).
- game-ci (game.ci) — run Unity tests + builds in CI headless.

## Mobile QA
- Apple — Instruments (developer.apple.com/documentation/instruments) — on-device perf/thermal.
- Device tier matrices — iOS device GPU/CPU tiers (Apple hardware docs / community charts).

## Release ops
- Apple — App Store Connect + TestFlight (developer.apple.com/app-store-connect; /testflight).
- Apple — App Review Guidelines (developer.apple.com/app-store/review/guidelines) — esp. §1.2 UGC, §2 performance, §5 privacy.
- Google Play — Console + policy (support.google.com/googleplay/android-developer) — **v1.1 (Android deferred)**.

## Crash analytics
- Sentry (docs.sentry.io) / Firebase Crashlytics (firebase.google.com/docs/crashlytics) — free tier (AUTH #017).

## Reference internal
- `qa/`, SPEC §6/§9/§11, SECURITY_CHECKLIST §8/§9, `governance/checkpoints/`.
