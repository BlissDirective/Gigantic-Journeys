---
name: BLOCKER
about: A ticket cannot progress and the Foreman cannot unblock it. The Owner or Coordinator decides.
title: "BLOCKER: <ticket id> — <one line>"
labels: ["blocker"]
assignees: ["BlissDirective"]
---

**Ticket:** `M0-XXXX-NN` (link the PR if one exists)
**Bot:** `gj-<role>`
**Blocked since:** YYYY-MM-DD

**What is blocked** (one or two lines):

**What was tried:**

**What unblocks it** (pick one): an AUTH decision (`#NNN`), a Coordinator decision, another ticket (`id`), Owner action (device, scan, account), other.

**Impact if unresolved for 48 h:**
