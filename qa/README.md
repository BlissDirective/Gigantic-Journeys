# `qa/`

Owner: gj-qa-release

Visual QA procedure (`VISUAL_QA.md`, M0-QA-02), editor smoke scripts, reports, evidence PNGs (≤ 2 MB each; clips are attached to PRs, not committed), motion-db reports (Bible §12).
