# `api/`

Owner: gj-platform

Vercel functions and Inngest durable workflows (scan → reconstruct → scene graph → journey → package). Skeleton in M0-PIPE-01. Secrets only via environment; `.env.example` lists names, never values.
