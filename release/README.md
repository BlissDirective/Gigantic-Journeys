# `release/`

Owner: gj-qa-release

TestFlight and Play Console procedures, store metadata, compliance forms (privacy labels, data safety). Uses a separate ops account with no production secrets. M5.
