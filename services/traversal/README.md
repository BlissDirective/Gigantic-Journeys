# `services/traversal/`

Owner: gj-scenegraph (validator), gj-gameplay (constants)

Summit designation, route generation with the deterministic reachability validator (Bible §10 contract: 85% margin, beat-1 verb set), vista selection. Loads `config/movement.json`. M0 stub via M0-MOVE-01, real work in M1.
