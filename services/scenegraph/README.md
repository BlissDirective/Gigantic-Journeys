# `services/scenegraph/`

Owner: gj-scenegraph

Python. Mesh cleanup, surface classification (Bible §4 classes), measurements, scale inference, semantic and material labels, affordance library, traversal-graph export. M1.
