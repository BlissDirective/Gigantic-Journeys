# `services/avatar/`

Owner: gj-avatar

Head (image-to-3D realistic mode) + parametric body merge, retopo, bake, auto-rig to the shared skeleton, validation with retry, source-photo deletion. Every entry point sits behind the consent gate. M2.
