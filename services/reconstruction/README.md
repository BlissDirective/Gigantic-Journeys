# `services/reconstruction/`

Owner: gj-capture

Upload pipeline and Luma integration (splat + mesh), scan quality gate, corpus intake and metadata stripping (M0-CAPT-01). Raw media never enters git.
