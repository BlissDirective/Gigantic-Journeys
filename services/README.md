# `services/`

Owner: see each subdirectory

Non-Unity services. One directory per service, each with its own pinned dependencies (`requirements.txt` with `==`, or `package.json` + `package-lock.json`), tests, and a README. CI lints and audits every package it finds.
