# M0 checkpoint — readiness review + AUDIT + STATUS

**2026-09-20 · Coordinator.** Combined **REVIEW M0 (readiness)**, **AUDIT**, and **STATUS** pass at the Owner's request. main @ `dde95df`. This is a *readiness* review — M0 is not yet complete; the design/governance/legal foundation is done and the engineering foundation has not started.

---

## STATUS (5 lines)
- **Milestone:** M0 (Harness), pre-build. Design arc for v1 is **feature-complete** (SPEC v1.9; AUTH #021–#026). No checkpoint date set — gated on Owner build kickoff.
- **Tickets:** 77 total — 69 open, 1 merged, 3 done, 1 blocked (M1-RES-01, Tier-2 research), 3 cancelled. This session: 6 design AUTHs + M4 decomposition (8 tickets) + legal + this review; 0 code merges.
- **Blockers:** build execution is gated on Owner-only setup (machine-user PAT #002; Unity account #008; Supabase #011; day-one corpus M0-OWNER-01). No agent is running yet.
- **Pending AUTH:** none (the M0 batch #008–#017 is approved; next free #027).
- **Risk watched:** iOS-in-Unity Gaussian-splat rendering (the Metal radix-sort at 30 fps) — the #1 technical unknown, de-risked by the M1-CAPT-03 spike.

## AUDIT — findings (ranked)
Scope note: no feature code, backend, or user data exists yet, so most runtime controls (RLS, signed URLs, consent gates, media-deletion paths, dependency CVEs, test-coverage/perf budgets) are **not yet applicable** — they land with the tickets that introduce them. What is auditable now is governance + supply-chain + secret hygiene.

| # | Sev | Finding | Disposition |
|---|---|---|---|
| A1 | Low | **5 open Dependabot PRs** (#1–5) bump CI actions (checkout, setup-python, setup-dotnet, cache, game-ci/unity-builder) by **major versions**; unreviewed, some behind main. Supply-chain hygiene, but major bumps can break CI. | Fix ticket **M0-REPO-07** drafted: merge each **only on green CI**, rebasing onto main first. Not auto-merged in this pass. |
| A2 | Low | **Tracker drift in `PROGRESS.md`:** "Blockers" + "Owner actions needed now" still reference the **retired 8-Bot / Foreman model** (superseded by AUTH #006: Builder + Coordinator + one Grok Operator) and ask to lock **decision 5**, already locked (AUTH #005, M0-OWNER-03 done). | **Fixed in this pass** (PROGRESS refreshed to the current operating model + completed locks). |
| A3 | Info | **`supabase/scripts/check_rls.py` referenced** (SECURITY_CHECKLIST §2.1) **but not present** — no tables exist yet. | Not a defect now; it lands with **M4-DATA-01** (first migrations). Tracked. |
| — | Clean | **Secret hygiene:** no secrets and no tracked `.env` (only `.env.example` placeholders); `.gitleaks.toml` + `secret-scan.yml` present. | ✓ |
| — | Clean | **Governance integrity:** AUTH log next-free consistent (#027), no pending rows; every protected-path edit this session cites its `APPROVED #n`; `config/movement.json` ↔ Bible §10 in sync; 77 tickets schema-valid; all workflows parse. | ✓ |

**5-line Owner summary:** The repo is clean — no secrets, governance consistent, all gates green, and the whole v1 design surface is specified. The only real hygiene item is 5 Dependabot CI-action bumps (ticket M0-REPO-07: merge on green CI). One tracker-drift bug (stale 8-Bot/Foreman references) was fixed in this pass. Everything else audited is not-yet-applicable because no app code exists — which is the true headline: **M0 engineering has not begun.**

## REVIEW M0 — readiness against the exit test
M0 exit: *one ticket goes creation → merged PR with a QA screenshot and no human typing; repo + CI green; Unity 6 URP project with splat renderer + controller scaffold; Inngest skeleton; every Bot's SKILLS.md; Unity on the QA VM; telemetry + correction schemas frozen; consent + retention drafted; vendor terms collected; design system ready to lock; M0 AUTH batch filed.*

| Exit component | State |
|---|---|
| Repo + CI green | ✅ Done (scaffold `fd4d44b` merged; 6 workflows + gate scripts) |
| Design system locked (decisions 1–10) | ✅ Done (AUTH #004/#005; M0-OWNER-02/03 done) |
| M0 AUTH batch filed + approved | ✅ Done (#008–#017) |
| Consent / retention / privacy / ToS drafted | ✅ Done (`legal/`; consent now V2-gated per #020; ToS at Apple-1.2 v0.2) |
| Telemetry + correction schemas frozen (M0-DATA-01) | ⬜ Open |
| Unity 6 URP project + splat renderer + controller scaffold (M0-UNITY-01/02/03) | ⬜ Open |
| Inngest skeleton (M0-PIPE-01) | ⬜ Open |
| Bot SKILLS handbooks (M0-SKILL-*) | ⬜ Open |
| Corpus intake path (M0-CAPT-01) + day-one corpus (M0-OWNER-01) | ⬜ Open |
| "One ticket creation→merged, no human typing" harness proof | ⬜ Not demonstrated |

**Verdict:** M0 is **~40% complete** — the governance, design, and legal scaffolding is done and *unblocks* the build; the engineering half (Unity, backend, pipeline, schemas, corpus, Bot skills) has not started. The gate is not technical debt; it is that **no agent is running and the Owner-only prerequisites are not yet set up.**

Per the six-item review format:
1. **Exit-test result:** not met (engineering components open); design/governance/legal components met.
2. **Security findings:** none critical; A1 (Dependabot) low; repo clean.
3. **Code-quality debt:** none (no code yet).
4. **UI/design deviations:** none (design system locked; no implementation to deviate).
5. **Unauthorized spend/accounts/design changes:** none — every design change this session carries an Owner AUTH (#021–#026); $0 spent.
6. **Three questions for the Owner** — see below.

## Three questions for the Owner (before build RESUME)
1. **Start M0 build execution now?** The critical path is **Unity scaffold (M0-UNITY-01/02) → reconstruction spike (M1-CAPT-03)** — the spike proves the iOS splat-render risk early. Proceed in that order, or stand up the full Unity harness (controller + tests) first?
2. **Which Owner-only prerequisites will you set up to unblock the build?** GitHub machine-user PAT (#002) + spin up the Grok Operator; Unity account (#008) + license secrets (M0-REPO-03); Supabase projects (#011); day-one corpus capture (M0-OWNER-01); App Store Connect key into CI (#005/M0-REPO-05). Recommended first: **machine-user PAT + Unity account** (unblocks the Builder + Unity CI immediately).
3. **Dependabot CI bumps (#1–5):** merge on green CI (recommended — supply-chain hygiene), or hold until the Unity CI actually exercises them?
