# `unity/`

Owner: gj-gameplay (project), gj-capture (Assets/Capture), gj-avatar (Assets/Avatar), gj-design (Assets/UI)

Unity 6 project. Created by ticket M0-UNITY-01. Pin the exact editor version in `ProjectSettings/ProjectVersion.txt` and in this README; CI, the QA VM, and the Mac runner must match it. `Assets/StreamingAssets/movement.json` must stay byte-identical to `config/movement.json` (CI checks).
